<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.maxwell.warehouse" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <!--
 The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but are recommended.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".screens.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".screens.MediaPlayerStreaming" />
        <activity android:name=".screens.TestRetrofit" />
        <activity android:name=".screens.FacebookLogin" />
        <activity android:name=".screens.FacebookShare" />
        <activity android:name=".screens.GlideDemo" />
        <activity android:name=".screens.ZoomInZoomOut" />
        <activity android:name=".screens.GitHubApiDemo" />
        <activity android:name=".screens.TTS" />
        <activity android:name=".screens.ButterKnifeDemo" />
        <activity android:name=".screens.ParallaxDemo" />
        <activity android:name=".screens.DrawerLayoutDemo" />

        <!-- FACEBOOK BEGIN -->
        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <provider
            android:name="com.facebook.FacebookContentProvider"
            android:authorities="com.facebook.app.FacebookContentProvider492944210879985"
            android:exported="true" />
        <!-- FACEBOOK END -->

        <!-- GOOGLE MAPS BEGIN -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".screens.GoogleMapsDemo"
            android:label="@string/title_activity_google_maps_demo" >
        </activity>

        <activity
            android:name=".screens.GoogleMapsDirections"
            android:label="@string/title_activity_google_maps_demo" >
        </activity>


        <!-- GOOGLE MAPS END -->
    </application>

</manifest>
